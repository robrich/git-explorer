{
  "name": "git-explorer",
  "version": "0.0.1",
  "private": true,
  "author": "Rob Richardson <robrich@robrich.org> (https://robrich.org/)",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "luxon": "^1.23.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/luxon": "^1.22.0",
    "@types/morgan": "^1.9.0",
    "@types/node": "^13.11.1",
    "tslint": "^6.1.1",
    "typescript": "^3.8.3"
  }
}
